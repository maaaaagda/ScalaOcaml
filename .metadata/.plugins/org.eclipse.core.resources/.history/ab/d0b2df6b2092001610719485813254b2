(function x -> x+x) 6;; 

let nth = (_, _, 0, _, _);;
let (-2, y, x, 1, 2) = nth;
let (a, b, c, d, f) = nth;;



let nt =  [(1,2);(0,1)];;
let [(a,b);(c,d)] = nt;;
let[(1,y);(x,1)] = nt;;

let k = (true, 5.0);;
let(f, g) = k;;
let(true, x) = k;;

let rec fib p= 
	match p with
	| 0->0
	| 1->1
	| _ -> fib(p-1)+fib(p-2) ;;

fib(42);;

let rec fibpom (a, b, n) = 
  if n = 0 then a else fibpom (b, (a + b), (n - 1));;
let fibn n = fibpom (0, 1, n);;
fibn 42;;
fib1(42);;

let rec replaceNth (lista, elemZmian, liczba) = 
	match (liczba, lista)  with
	| (_,[])->[]
	| (0, _) -> elemZmian:: replaceNth(List.tl lista, elemZmian, liczba)
	| _ -> List.hd lista::replaceNth(List.tl lista, elemZmian, liczba-1);;
	
	replaceNth(5::8::9::1::3::4::5::[],100, 2);;
	
	